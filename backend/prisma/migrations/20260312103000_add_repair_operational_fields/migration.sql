DO $$
BEGIN
  CREATE TYPE "CurrencyCode" AS ENUM ('ARS', 'USD');
EXCEPTION
  WHEN duplicate_object THEN NULL;
END $$;

ALTER TABLE IF EXISTS "RepairRecord"
  ADD COLUMN IF NOT EXISTS "performedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  ADD COLUMN IF NOT EXISTS "unitKilometers" INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "currency" "CurrencyCode" NOT NULL DEFAULT 'ARS';
